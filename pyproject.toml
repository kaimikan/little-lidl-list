[project]
name = "little-lidl-list"
version = "0.1.0"
description = "Scan Lidl Bulgaria offers for healthy items suited for a training man"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40",
    "rich>=13.0",
    "flask>=3.0",
]

[tool.setuptools]
# Only ship the package; summaries/ and systemd/ are data dirs, not packages.
packages = ["lidl"]

[project.scripts]
lidl = "lidl.cli:main"
lidl-summary = "lidl.summary:main"
lidl-mealplan = "lidl.mealplan:main"
